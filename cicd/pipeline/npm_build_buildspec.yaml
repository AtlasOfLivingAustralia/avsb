version: 0.2
###
# This build project builds the AVSB site files ready for upload to s3

env:
  shell: bash

phases:
  install:
    on-failure: ABORT
    runtime-versions:
      nodejs: 18
    commands:
      - echo installing...
      - npm i
  pre_build:
    commands:
      - echo running tests...
      - npm run test
  build:
    commands:
      - echo building...
      - npm run build:$ENVIRONMENT

artifacts:
  base-directory: ./dist
  files:
    - '**/*'
